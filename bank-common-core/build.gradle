plugins {
    id 'bank.spring-library'
}

dependencies {
    api project(':bank-common')

    implementation 'org.springframework:spring-webmvc'

    testImplementation 'tools.jackson.core:jackson-databind'
    implementation 'jakarta.servlet:jakarta.servlet-api'
}
